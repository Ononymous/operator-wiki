# 01-self

Personal / private notes — identity, contacts, health, reminders. The agent's `Query rules` (see `CLAUDE.md`) instruct it not to auto-read this folder. Reads happen only on explicit request.

Excluded from the public-repo deny-list and from the vector index by default.
