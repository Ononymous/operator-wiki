# 00-inbox

Fast capture. Drop unsorted notes here — clipped articles, mid-conversation jottings, anything that needs a home but you haven't decided where yet.

Use `/capture [text]` (see `CLAUDE.md`) to drop a timestamped note. Use `/sort-inbox` periodically to route everything to the right folder. The agent will ask before moving anything ambiguous.
