# CRITICAL_FACTS

The minimum-viable operator state — always loaded into the agent's context. Aim for ~120 tokens; if it grows beyond ~200, move detail into a linked page and keep only pointers here.

## Identity (TODO)

<!-- One or two lines: who you are, primary role, primary location. -->

## Active commitments (TODO, update weekly)

<!-- 2–4 short bullet points: current project, immediate deadline, top blocker. -->

## Working assumptions

- Wiki layout: `03-research/wiki/{concepts-kb,concepts-llms,concepts-systems,entities,sources-kb,sources-llms,sources-systems,synthesis}/`
- Fast capture: `00-inbox/`
- Active tasks: `06-tasks/now.md`
- Session log location: `logs/`
- Slash commands available: `/resume`, `/save`, `/ingest`, `/synth`, `/sort-inbox`, `/now`, `/capture`, `/query` (see `CLAUDE.md`)

## Conventions

- This file is a **routing page** — no `## For future Claude` preamble; excluded from the vector index.
