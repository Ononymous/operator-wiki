Create a note in 00-inbox/ for: $ARGUMENTS

- Filename: YYYY-MM-DD-[2-3-word-slug].md using today's date
- Frontmatter: title, tags: [], created: today, type: note
- Body: the captured content, verbatim or lightly cleaned
- No routing, no linking, no analysis — just capture fast

Confirm with the filename only.
